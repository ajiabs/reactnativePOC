import Form, {Input, Button, Select} from './form';
import Partials, {AlertStatus, BackgroundWrapper, Logo, LeftMenu} from './partials';
import Text, {TextFont, Heading} from './text';

export {
    Form, Input, Button,
    Partials, AlertStatus, BackgroundWrapper, Logo, LeftMenu,
    Text, TextFont, Heading, Select
}

export default {
    Form, Input, Button,
    Partials, AlertStatus, BackgroundWrapper, Logo, LeftMenu,
    Text, TextFont, Heading, Select
}